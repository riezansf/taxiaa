== Mockup Visualization Tools ==

Init :
- Show bandung map with border

# CHOOSE DATA
    Period : StartDate - End Date
        Week : Weekdays &|| Weekend
        Days : Pagi / Siang / Sore / Malam / Dini Hari
    * all data (argo_gps_join_XX.csv) stored in mysql, don't forget to recheck     
    * Load data button
        * Show All Origin Ponint
        * Show All Destination Ponint

# Clustering O-D Pair
    Eps : ? 
    MinPts : ?
    Grid Size : Don't Use / 50m / 100m / 200m / 500m
    * process :
        - query from mysql where choosen period usinng AJAX
        - if use grid {
            mapPointToGrid();
            calculateGridCentroid();
            dbscan(eps,minPts);
            countTripBetweenODPair();
            showResult();
          }else{
            dbscan(eps,minPts);
            showResult();
            *OD Pair Not Available
          }
          
    * Filter Result
        * Show/Hide Table Count Rank
        * Show/Hide Clustered Origin Point
        * Show/Hide Clustered Destination Point
        * Show/Hide Clustered Grid Origin w/ centroid
        * Show/Hide Clustered Grid Destination w/ centroid
        * Show/Hide Arrow-line between grid

# O-D Sequential Pattern Mining
    Grid
        Size : 50m / 100m / 200m / 500m 
        * Process
            - query from mysql where choosen period usinng AJAX
            - mapPointToGrid(); assign grid number to every points 
            - generate grid OD pair (ITEM SET) 
            - Run PrefixSpan
            - Visualize Result
            - Cluster Result??

        * Filter Result
            * Show/Hide Table Result Rank, Support & Confidence
            * Show/Hide Origin Point
            * Show/Hide Destination Point
            * Show/Hide Arrow-line between grid centroid (average point lat-long)
        
    Use Previous Cluster Result
        Enable when clustering OD process done
        * Process
    
    
    